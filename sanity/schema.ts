// This folder contains schema definitions for a separate Sanity Studio project.
// The Next.js app itself does NOT require the `sanity` (studio) package.
export { default as game } from "./schemas/game";
export { default as dailyGame } from "./schemas/dailyGame";
export { default as userGuess } from "./schemas/userGuess";

